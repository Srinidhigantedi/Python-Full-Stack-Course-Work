#from package_name.file_name import function_name

from CALCULATOR.add import add
from CALCULATOR.sub import sub
from CALCULATOR.mul import mul
from CALCULATOR.div import div

print(add(4,5))
print(sub(4,5))
print(mul(4,5))
print(div(4,5))

#calculator is the package ,consisting of the varies modules
#module is the collection of functions or classes or variables
#function is a reusable block of code 
